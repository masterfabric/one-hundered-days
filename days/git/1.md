# Scenario 1: Initializing a Repository

## 📝 Today's Tasks

1.  **Create a new project directory:** Make a new folder for your project.
2.  **Navigate into the directory:** Change your current directory to the new one.
3.  **Initialize a Git repository:** Use the `git init` command to start tracking the directory.
4.  **Verify the repository:** Check for the hidden `.git` directory to confirm that the repository was created.

## 📖 Task Explanations

A Git repository is a data structure that stores the history of your project. The `git init` command creates a new Git repository in your current directory. This is the first step to start tracking your project's changes with Git. It creates a hidden `.git` subdirectory where Git stores all of its metadata.

## 💡 What You'll Gain

You will learn how to create a new Git repository, which is the starting point for any project that you want to manage with Git.

## 📚 Dictionary for Developers

*   **`git init`:** A command that creates a new Git repository.
*   **Repository:** A directory where Git stores all the files and history of your project.
