# Scenario 3: Checking Status & History

## 📝 Today's Tasks

1.  **Check the status:** Use the `git status` command to see which files have been modified, which are staged, and which are untracked.
2.  **View the commit history:** Use the `git log` command to see a list of all the commits that have been made.
3.  **Explore log options:** Try out different options for `git log`, such as `--oneline` and `--graph`.

## 📖 Task Explanations

To work effectively with Git, you need to be able to see what's going on. The `git status` command is your window into the current state of your repository. It tells you what changes you have made that haven't been saved yet. The `git log` command allows you to look back in time and see the history of your project.

## 💡 What You'll Gain

You will learn how to inspect your repository to understand what is happening. This is essential for making informed decisions about what to do next.

## 📚 Dictionary for Developers

*   **`git status`:** A command that shows the state of the working directory and the staging area.
*   **`git log`:** A command that shows the commit history.
