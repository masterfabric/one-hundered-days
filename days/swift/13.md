# Day 13: Generics & Error Handling - Typed Throws & Result

## 📝 Today's Tasks

1.  **Adopt Typed Throws:** Declare `func load() throws(NetworkError)` for a precise error type (Swift 6).
2.  **Use the `Result` Type:** Represent success/failure as a `Result<Success, Failure>` value.
3.  **Convert Between Them:** Bridge `Result` and throwing with `get()` and `Result(catching:)`.
4.  **Propagate Errors:** Rethrow with `try` and let errors bubble up the call stack.

## 📖 Task Explanations

Swift 6 refines error handling with typed throws: instead of an untyped `any Error`, a function can declare exactly which error type it throws, giving callers precise, exhaustive handling and better performance. The `Result` type captures success or failure as a first-class value you can store, pass, and transform — useful for completion-style APIs and for holding an outcome without immediately handling it. Knowing how to move between throwing functions and `Result`, and when each fits, lets you design clean, predictable error-handling boundaries in your codebase.

## 💡 What You'll Gain

You will use typed throws and `Result` to make error handling precise and composable, choosing the right tool for each boundary in your code.

## 📚 Dictionary for Developers

*   **Typed Throws:** Declaring the specific error type a function can throw (`throws(E)`).
*   **`Result`:** An enum with `.success`/`.failure` cases representing an operation's outcome.
*   **Propagation:** Letting an error travel up the call stack via `try`.
*   **`Result(catching:)`:** An initializer that runs throwing code and wraps the outcome.
